# Fallback configuration. Real config is stored in trading_bot.db (settings table).
# These values are used only on first run to seed the database if present.

BINANCE_TESTNET_API=
BINANCE_TESTNET_SECRET=
BINANCE_LIVE_API=
BINANCE_LIVE_SECRET=

HF_TOKEN=
GNEWS_API=

TELEGRAM_TOKEN=
TELEGRAM_CHAT_ID=
